---
sidebar_position: 9
---

import FigmaEmbed from '@site/src/components/FigmaEmbed';
import StorybookEmbed from '@site/src/components/StorybookEmbed';

# Select

Displays a list of options for the user to pick from — triggered by a button.

<FigmaEmbed url="https://www.figma.com/file/YOUR_FILE_ID/Select" />

<StorybookEmbed storyId="components-select--default" />

## Usage

```tsx
import {
  Select,
  SelectTrigger,
  SelectContent,
  SelectItem,
  SelectValue,
} from '@emmettorama/substance';
```

```tsx
<Select>
  <SelectTrigger className="w-[180px]">
    <SelectValue placeholder="Theme" />
  </SelectTrigger>
  <SelectContent>
    <SelectItem value="light">Light</SelectItem>
    <SelectItem value="dark">Dark</SelectItem>
    <SelectItem value="system">System</SelectItem>
  </SelectContent>
</Select>
```

## API

### Select

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | Controlled value |
| `defaultValue` | `string` | — | Uncontrolled value |
| `onValueChange` | `(value: string) => void` | — | Change handler |
| `disabled` | `boolean` | `false` | Disabled state |

### SelectItem

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string` | — | Item value |
| `disabled` | `boolean` | `false` | Disabled state |
